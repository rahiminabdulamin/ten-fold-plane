{
  "name": "@plane/copilot",
  "version": "1.4.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env --env-file=.env src/main.ts",
    "start": "tsx src/main.ts",
    "test": "vitest run",
    "check:types": "tsc --noEmit",
    "check:format": "oxfmt --check .",
    "fix:format": "oxfmt ."
  },
  "dependencies": {
    "@copilotkit/runtime": "1.69.0"
  },
  "devDependencies": {
    "@plane/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
